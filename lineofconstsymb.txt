.model tiny
.code
org 100h
VIDEOSEG   	equ 0b800h
Start:     	mov cl, ds: byte ptr [80h]
		dec cl
		mov ax, VIDEOSEG
            	mov es, ax
           	mov bx, 80*2*23d
		mov di, 82h
		mov si, 0
		
		l:
		mov al, ds: byte ptr [di];input str
            	mov ah, 5dh
		add bx, 2
            	mov es: word ptr [bx], ax ;place
		inc di
		loop l

	    	mov ax, 4c00h
	    	int 21h
end         	Start
